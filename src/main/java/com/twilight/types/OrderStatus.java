package com.twilight.types;

public enum OrderStatus {
    outlet_pending,
    outlet_rejected,
    outlet_approved,
    driver_pending,
    driver_assigned,
    preparing,
    on_the_way,
    delivered,
}
